rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
    match /bookings/{bookingId} {
       // Minimal rule to allow public booking creation
      allow create: if true;
    }
  }
}
